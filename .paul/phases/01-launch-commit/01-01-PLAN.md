---
phase: 01-launch-commit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/contact/route.ts
  - src/app/api/farms/submit/route.ts
  - src/app/robots.ts
  - src/app/sitemap.ts
  - supabase/migrations/006_contact_messages_pending_farms.sql
  - src/components/farms/AddFarmForm.tsx
  - src/components/kontakt/ContactForm.tsx
  - src/app/layout.tsx
  - package.json
  - package-lock.json
autonomous: false
---

<objective>
## Goal
Verify the build passes, commit all pending work from the last session, and apply migration 006 to Supabase.

## Purpose
Five uncommitted features are production-ready but undeployed: contact form API, farm submission API, sitemap.ts, robots.ts, and the Supabase migration that backs them. Committing and deploying this closes the last session's work and unblocks the monetization milestone.

## Output
- All 10 pending files committed to master
- Migration 006 applied in Supabase (contact_messages + pending_farms tables)
- Vercel deployment triggered automatically on push
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## What's uncommitted
- `src/app/api/contact/route.ts` — POST handler: validates, saves to contact_messages, optional Resend email
- `src/app/api/farms/submit/route.ts` — POST handler: validates, saves to pending_farms, optional Resend email
- `src/app/robots.ts` — robots.txt (disallows /admin, /api, /auth)
- `src/app/sitemap.ts` — dynamic sitemap with all static routes + 4,009 farm slugs
- `supabase/migrations/006_contact_messages_pending_farms.sql` — creates contact_messages and pending_farms tables with RLS
- `src/components/farms/AddFarmForm.tsx` — wired to /api/farms/submit
- `src/components/kontakt/ContactForm.tsx` — wired to /api/contact
- `src/app/layout.tsx` — minor update
- `package.json` + `package-lock.json` — resend dependency added
</context>

<acceptance_criteria>

## AC-1: Build passes
```gherkin
Given all pending files are staged
When npm run build runs
Then it completes with exit code 0 and no TypeScript or ESLint errors
```

## AC-2: All work committed and pushed
```gherkin
Given build passes
When git commit and push run
Then all 10 pending files appear in the new commit on master
And Vercel starts a new deployment automatically
```

## AC-3: Migration applied
```gherkin
Given migration 006 SQL is available
When it is executed in the Supabase SQL Editor
Then contact_messages and pending_farms tables exist with correct schema and RLS policies
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify build passes</name>
  <files>no files modified — read-only verification</files>
  <action>
    Run: npm run build
    This runs TypeScript type-checking + ESLint + Next.js compilation.
    Read full output. If errors appear, report them — do NOT silently skip.
    Do not proceed to commit if build fails.
  </action>
  <verify>Exit code 0. No "Type error" or "ESLint:" lines in output.</verify>
  <done>AC-1 satisfied: Build exits cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit all pending files</name>
  <files>
    src/app/api/contact/route.ts,
    src/app/api/farms/submit/route.ts,
    src/app/robots.ts,
    src/app/sitemap.ts,
    supabase/migrations/006_contact_messages_pending_farms.sql,
    src/components/farms/AddFarmForm.tsx,
    src/components/kontakt/ContactForm.tsx,
    src/app/layout.tsx,
    package.json,
    package-lock.json
  </files>
  <action>
    Stage each file explicitly by name (not git add -A or git add .).
    Commit with message:
      feat: contact API, farm submit API, sitemap, robots, migration 006

      - POST /api/contact — saves to contact_messages, optional Resend email
      - POST /api/farms/submit — saves to pending_farms, optional Resend email
      - sitemap.ts — dynamic sitemap with static routes + all farm slugs
      - robots.ts — disallows /admin, /api, /auth
      - migration 006 — contact_messages + pending_farms tables with RLS
      - AddFarmForm + ContactForm wired to real APIs
      - resend dependency added

    Then push to origin master.
  </action>
  <verify>
    git log --oneline -1 shows the new commit.
    git status shows clean working tree.
  </verify>
  <done>AC-2 satisfied: All 10 files committed, pushed to master</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Apply migration 006 in Supabase SQL Editor</action>
  <instructions>
    Claude has committed the migration file but cannot run it directly (Supabase direct DB is IPv6-only).

    1. Open: https://supabase.com/dashboard/project/eqrmwkyzllkpkuqhwswk/sql
    2. Click "New query"
    3. Paste the contents of: supabase/migrations/006_contact_messages_pending_farms.sql
    4. Click "Run"
    5. Confirm: no errors, output shows "Success"
  </instructions>
  <verification>
    After you type "done", I'll verify by checking that the contact form API responds correctly.
  </verification>
  <resume-signal>Type "done" when the migration has been applied successfully</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/lib/farms.ts (data layer — stable)
- src/lib/supabase.ts (Supabase client — stable)
- Any page components (out of scope for this plan)
- Any existing API routes (only adding new ones)

## SCOPE LIMITS
- This plan is commit-only — no new features, no refactoring
- Do not add Resend env vars or configure email yet (Phase 4)
- Do not modify the migration SQL (apply as-is)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run build exits with code 0
- [ ] git log shows new commit with all 10 files
- [ ] git status is clean
- [ ] Migration 006 applied (user confirmed)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Build passes cleanly
- All pending work committed and pushed to master
- Vercel deployment triggered
- Migration 006 applied — contact_messages and pending_farms tables exist
</success_criteria>

<output>
After completion, create `.paul/phases/01-launch-commit/01-01-SUMMARY.md`
</output>
